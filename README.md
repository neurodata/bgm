# Bisected graph matching improves automated neuron pairing between hemispheres in the *Drosophila* larva

## Creating an environment (for me at least)
`poetry env use python3.9`
`source ./.venv/bin/activate`
`poetry install`